# Print fixtures

Markdown in `fixtures/round-trip/` is the input.
Expected print HTML is generated by `@mdword/renderer`.

For PDF regression, export a fixture through Electron `printToPDF` and compare page count and header text. Automated screenshot comparison is post-MVP (see roadmap).
